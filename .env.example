# OpenRouter API 配置（必需）
OPENROUTER_API_KEY=your-api-key-here
OPENROUTER_API_URL=https://openrouter.ai/api/v1
OPENROUTER_APP_NAME=video_note_generator_v2
OPENROUTER_HTTP_REFERER=https://github.com

# Unsplash API 配置（可选，用于图片）
UNSPLASH_ACCESS_KEY=your-unsplash-access-key-here
UNSPLASH_SECRET_KEY=your-unsplash-secret-key-here

# 模型配置
AI_MODEL=google/gemini-pro
# 视觉选帧模型（需支持看图；默认可与 AI_MODEL 相同）
VISION_MODEL=google/gemini-2.0-flash-exp:free
WHISPER_MODEL=medium

# 输出目录配置
OUTPUT_DIR=generated_notes
CACHE_DIR=.cache
LOG_DIR=logs

# 内容生成配置
MAX_TOKENS=2000
CONTENT_CHUNK_SIZE=2000
TEMPERATURE=0.7
TOP_P=0.9

# 笔记样式配置
USE_EMOJI=true
TAG_COUNT=5
MIN_PARAGRAPHS=3
MAX_PARAGRAPHS=6

# 代理配置（可选）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# Cookies 配置（可选，用于访问需要登录的视频）
# 如果需要下载需要登录的视频（如B站会员视频），可以配置cookies文件
# 使用浏览器插件导出cookies（Netscape格式）：
#   Chrome/Edge: 安装 "Get cookies.txt LOCALLY" 插件
#   Firefox: 安装 "cookies.txt" 插件
# 导出后将文件路径填写到下面：
# COOKIE_FILE=/path/to/cookies.txt

# 调试配置
DEBUG=false
LOG_LEVEL=INFO

# FFmpeg 配置（Windows 用户需要）
# FFMPEG_PATH=C:\\path\\to\\ffmpeg.exe
